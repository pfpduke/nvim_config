git config --global core.autocrlf true   # Keep LF, even on Windows (good for cross-platform)

winget install --id=sharkdp.fd           # fd is very nice for telescope

winget install zig.zig                   # Install zig Compiler

